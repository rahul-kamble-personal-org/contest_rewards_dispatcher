{
    "name": "partition-processor-lambda",
    "version": "1.0.0",
    "description": "Partition Processor Lambda Function",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.645.0",
        "aws-sdk": "^2.1691.0",
        "winston": "^3.14.2"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.119",
        "@types/node": "^18.16.18",
        "typescript": "^5.1.6"
    }
}
